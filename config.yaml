model:
  tokenizer_training_size: 0.3
  train_test_ratio: 0.9
  vocab_size: 1000
  block_size: 256 # T (context window)
  batch_size: 32 # B (data points per batch)
  d_model: 128 # C (embeddings dimension)
  num_heads: 4
  num_transformer_blocks: 6
  dropout_rate: 0.1
  device: "cuda"
